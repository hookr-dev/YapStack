# SPDX-License-Identifier: AGPL-3.0-only
[package]
name = "yapstack-entitlements"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true
description = "The YapStack entitlements seam (ENTITLEMENTS_SEAM.md): quantitative-only Limit vocabulary, the TenantLimitSource contract, and the AllowAll / StoredLimits sources. Limits only, never features — enforced by the type system."

[dependencies]
serde = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
async-trait = { workspace = true }
sqlx = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
tokio = { workspace = true }
