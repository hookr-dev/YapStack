# Rust
target/
**/*.rs.bk

# Node
node_modules/
.pnpm-store/
dist/
*.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Binaries
binaries/

# Test fixtures
tests/fixtures/*.wav

# Whisper models
models/
*.bin

# Databases — never commit personal/local DBs or their sidecars/backups
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm
*.backup
spike.db*

# Environment
.env
.env.local
.env.*.local

# Tauri
apps/desktop/src-tauri/gen/

# Apple signing assets
*.p12
*.cer
*.mobileprovision
*.provisionprofile

# Generic secrets
**/secrets.json
**/credentials.json
**/*.secret
**/*.private

# Logs
*.log

# Local Claude Code / agent scratch state
.claude/scheduled_tasks.lock
.claude/settings.local.json

# GoalBuddy board scaffolding — process state (local board URL, receipts), not
# stable docs. User-facing docs live under docs/ and are routed via INDEX.md.
docs/goals/

# Codex-style agent scratch (mirrors .claude/skills/ — already tracked there)
.agents/

# Generated types (committed to repo for CI; regenerated by `pnpm tauri dev`)

# GoalBuddy goal boards are local agent working state, not repo docs
docs/goals/

# Local business-strategy docs + spike harness — keep out of the public remote
# (specific paths only; legitimate plan docs under docs/plans/ stay committed)
docs/plans/ENTITLEMENTS_SEAM.md
docs/plans/research/
scripts/spike-crsqlite/
scripts/spike-dbswap/
